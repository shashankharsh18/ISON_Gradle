/*
 * This file was generated by the Gradle 'init' task.
 *
 * This generated file contains a sample Java Library project to get you started.
 * For more details take a look at the Java Libraries chapter in the Gradle
 * User Manual available at https://docs.gradle.org/6.3/userguide/java_library_plugin.html
 */

plugins {
    // Apply the java-library plugin to add support for Java Library
    id 'java-library'
}

repositories {
    // Use jcenter for resolving dependencies.
    // You can declare any Maven/Ivy/file repository here.
    jcenter()
}

dependencies {
    // This dependency is exported to consumers, that is to say found on their compile classpath.
    api 'org.apache.commons:commons-math3:3.6.1'

    // This dependency is used internally, and not exposed to consumers on their own compile classpath.
    implementation 'com.google.guava:guava:28.2-jre'

    // Use JUnit test framework
    testImplementation 'junit:junit:4.12'
    
    // https://mvnrepository.com/artifact/org.seleniumhq.selenium/selenium-java
	compile group: 'org.seleniumhq.selenium', name: 'selenium-java', version: '3.141.59'
	
	// https://mvnrepository.com/artifact/org.testng/testng
	compile group: 'org.testng', name: 'testng', version: '6.14.3'
	
	//Use Screenshot framework
	compile group: 'commons-io', name: 'commons-io', version: '2.8.0'
}

//def trainee = project.hasProperty("trainee")
//def trainer = project.hasProperty("trainer")
def build = project.hasProperty("build")
//def smoke = project.hasProperty("smoke")  
//def sdhead = project.hasProperty("sdhead")
//def radmin = project.hasProperty("radmin")


test{
 	useTestNG(){
  	if(build) 
  		{
            suites 'testNG.xml'
        }
		
    
	//if(trainer) 
  		//{
          //  suites 'testNGTrainer.xml'
        //}
		
       //if(smoke) {
           // suites 'testNGTRNCLNTGUESTSHO.xml'
       //}
	   //if(sdhead)
  		//{
           // suites 'testNGSDHead.xml'
        //}
		//if(radmin) 
  		//{
          //  suites 'testNGRAdmin.xml'
        //}
  		  	
  		useDefaultListeners = true
  		
 		outputDirectory = file("$projectDir/TestNGReports")
  		
 			}
 		testLogging.showStandardStreams = true
 			
 		reports.html.setDestination(file("$projectDir/GradleReports"))
	}